import React from 'react';
import MainTemplate from '../../components/main/MainTemplate';

interface MainPageProps {}

const MainPage: React.SFC<MainPageProps> = () => {
  return <MainTemplate>으으으으으</MainTemplate>;
};

export default MainPage;
