import React from 'react';
import GalleryViewerContainer from '../base/GalleryViewerContainer';

type CoreProps = {};

const Core: React.SFC<CoreProps> = () => {
  return (
    <React.Fragment>
      <GalleryViewerContainer />
    </React.Fragment>
  );
};

export default Core;
