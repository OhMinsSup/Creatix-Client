import React from 'react';
import PublishThumbnail from '../../components/write/publish/PublishThumbnail';

interface PublishThumbnailContainerProps {}
const PublishThumbnailContainer: React.SFC<
  PublishThumbnailContainerProps
> = () => {
  return <PublishThumbnail />;
};

export default PublishThumbnailContainer;
