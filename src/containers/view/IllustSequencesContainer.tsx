import React from 'react';
import IllustSequences from '../../components/view/illust/IllustSequences';

interface IllustSequencesContainerProps {}
const IllustSequencesContainer: React.SFC<
  IllustSequencesContainerProps
> = () => {
  return <IllustSequences />;
};

export default IllustSequencesContainer;
